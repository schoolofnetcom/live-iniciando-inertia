import React from 'react';
import Layout from './../Layout';

const Contact = ({title}) => {
    return (
        <Layout title={title}>
            <h1>Contact Page</h1>
        </Layout>
    );
}

export default Contact;
