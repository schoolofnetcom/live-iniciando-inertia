import React from 'react';
import Layout from './../Layout';

const Home = ({title}) => {
    return (
        <Layout title={title}>
            <h1>Home Page</h1>
        </Layout>
    );
}

export default Home;
